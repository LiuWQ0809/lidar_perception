# ROS2 日志配置
# 日志级别: DEBUG, INFO, WARN, ERROR, FATAL

# 默认日志级别
logger_level=WARN

# 特定节点的日志级别
# logger.fusion_perception_node=INFO
# logger.SensorFusion=WARN
# logger.MultiObjectTracker=WARN
# logger.TensorRTDetector=WARN

# 日志输出
# console_output_format=[{severity}] [{time}] [{name}]: {message}
